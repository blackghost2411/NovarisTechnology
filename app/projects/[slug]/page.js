import Link from 'next/link';import {projects} from '../../../lib/data';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export function generateMetadata({params}){const p=projects.find(x=>x.slug===params.slug);return {title:`${p?.title||'Project'} | Novaris Technology`}}
function Nav(){return <header className="nav"><div className="navin"><Link href="/" className="brand"><div className="logo">N</div><div><h1>Novaris Technology</h1><p>Case Study</p></div></Link><nav className="links"><Link href="/">Home</Link><Link href="/#projects">Projects</Link><Link className="btn" href="/start-project">Start a Project →</Link></nav></div></header>}
function Stat({value,label}){return <div className="metric"><b>{value}</b><p>{label}</p></div>}
export default function Project({params}){const p=projects.find(x=>x.slug===params.slug)||projects[0];return <main className="site gridbg"><Nav/><section className="container pagehero"><span className="pill">{p.category} case study</span><h1>{p.title}</h1><p className="lead">{p.summary}</p><div className="hero-metrics"><Stat value="12 w" label="Typical delivery plan"/><Stat value="4+" label="Core modules"/><Stat value="24/7" label="Monitoring ready"/><Stat value="100%" label="Responsive interface"/></div></section><section className="container detail"><div><img src={p.image} alt={p.title}/></div><div className="formbox" style={{padding:28}}><h2>Overview</h2><p className="muted">{p.summary}</p><h3>Challenge</h3><p className="muted">{p.challenge}</p><h3>Solution</h3><p className="muted">{p.solution}</p><h3>Technology Stack</h3><div className="chips">{p.tags.map(t=><span className="chip" key={t}>{t}</span>)}</div></div></section><section className="container section"><div className="section-head"><div><span className="eyebrow">OUTCOMES</span><h2>What this type of solution is designed to improve.</h2></div></div><div className="process">{p.results.map((r,i)=><div className="step" key={r}><span>{String(i+1).padStart(2,'0')}</span><h3>{r}</h3><p className="muted">Clear workflow design, strong data visibility and a premium interface reduce operational friction.</p></div>)}</div></section><section className="container section"><div className="finalcta"><span className="pill">Build something similar</span><h2>Turn your idea into a serious product plan.</h2><p className="muted">Share your business requirements and we will shape a clear direction for the platform, architecture and launch plan.</p><Link className="btn" href="/start-project">Start a Project →</Link></div></section><footer className="footer">© 2026 Novaris Technology.</footer></main>}
