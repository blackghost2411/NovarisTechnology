import { PageShell, Hero, SectionTitle, Card } from '../../components/Frame'
import { projects } from '../../content/site'
export const metadata = { title: 'Projects | Novaris Technology' }
export default function Projects(){return <PageShell><Hero kicker="Portfolio" title="Enterprise-grade showcase projects with real technical depth." subtitle="The following case studies are solution showcases that demonstrate Novaris capabilities across enterprise software, AI, cloud, healthcare, logistics and education technology."/><section className="section"><SectionTitle kicker="Case studies" title="Digital platforms designed for scale."/><div className="grid cols-2">{projects.map(p=><Card id={p.slug} key={p.slug} className="project"><div><div className="visual"/><span className="eyebrow">{p.type}</span><h3>{p.title}</h3><p>{p.desc}</p><p><b>Outcome:</b> {p.outcome}</p><div className="tags">{p.tech.map(t=><span className="tag" key={t}>{t}</span>)}</div></div></Card>)}</div></section></PageShell>}
