import {projects} from '../data';import {Nav,Footer,ProjectImage} from '../components';import Link from 'next/link';
export const metadata={title:'Projects | Novaris Technology'}
export default function ProjectsPage(){return <><Nav/><section className="pagehero"><span className="badge">Portfolio showcase</span><h1>Enterprise platforms, AI systems and cloud products.</h1><p className="lead">A curated collection of premium showcase projects that reflect the type of secure, scalable and business-focused systems Novaris Technology can build.</p></section><section className="section"><div className="projectgrid">{projects.map((p)=><Link href={`/projects/${p[7]}`} className="project" key={p[0]}><div className="shot realshot"><ProjectImage slug={p[7]} title={p[0]}/></div><div className="projectbody"><span className="badge small">{p[2]}</span><h3>{p[0]}</h3><p>{p[1]}</p>{p.slice(3,7).map(t=><span className="pill" key={t}>{t}</span>)}<p className="caseLink">View Case Study →</p></div></Link>)}</div></section><Footer/></>}
