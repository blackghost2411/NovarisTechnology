import { company, services, projects, training, values } from '../../content/site';
export const metadata = { title: 'About | Novaris Technology' };
export default function Page(){
 const name='about';
 return <main className="page wrap"><span className="eyebrow">Novaris Technology</span><h1>About</h1>{name==='about'&&<><p>{company.summary}</p><div className="split"><div className="panel"><h2>Mission</h2><p>{company.mission}</p><h2>Vision</h2><p>{company.vision}</p></div><div className="values">{values.map(v=><div className="value" key={v}>{v}</div>)}</div></div></>}{name==='services'&&<div className="cards">{services.map(s=><article className="card" key={s.title}><span className="tag">{s.tag}</span><h3>{s.title}</h3><p>{s.desc}</p><div className="list">{s.points.map(x=><span className="chip" key={x}>{x}</span>)}</div></article>)}</div>}{name==='projects'&&<div className="projects">{projects.map(p=><article className="project" key={p.title}><div className="project-art"/><div className="project-body"><span className="tag">{p.category}</span><h3>{p.title}</h3><p>{p.desc}</p><div className="list">{p.tech.map(t=><span className="chip" key={t}>{t}</span>)}</div><p>{p.outcome}</p></div></article>)}</div>}{name==='training'&&<div className="training-grid">{training.map(t=><article className="card" key={t.area}><span className="tag">Training</span><h3>{t.area}</h3><div className="list">{t.items.map(i=><span className="chip" key={i}>{i}</span>)}</div></article>)}</div>}{name==='contact'&&<div className="contact"><div className="panel"><h2>Contact Novaris</h2><p>Email: {company.email}</p><p>Location: {company.location}</p></div><form className="panel"><input className="input" placeholder="Your name"/><input className="input" placeholder="Email"/><textarea className="input" placeholder="Message"/><button className="btn" type="button">Send</button></form></div>}{name==='privacy'&&<div className="legal panel"><p>Novaris Technology respects privacy and uses submitted contact information only to respond to inquiries, provide requested services, and improve communication.</p><h2>Data we collect</h2><p>Contact details, project requirements, and technical information voluntarily submitted through forms or email.</p><h2>How data is used</h2><p>To communicate with clients, prepare technical proposals, and deliver requested services.</p></div>}{name==='terms'&&<div className="legal panel"><p>By using this website, visitors agree to use it responsibly and not attempt to disrupt, abuse, scrape, or attack the service.</p><h2>Services</h2><p>All project work is subject to written agreement, scope, timeline, and payment terms.</p><h2>Responsible use</h2><p>Novaris does not support spam, phishing, illegal access, harmful automation, or policy-violating cloud workloads.</p></div>}</main>
}
